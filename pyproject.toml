[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[dependency-groups]
dev = [
    "click>=8.4.1",
    "httpx>=0.28.1",
    "regex>=2026.5.9",
    "tomlkit>=0.15.0",
]

[tool.ruff]
src = ["tracking"]

[tool.ruff.lint]
extend-select = ["U", "TYP", "F", "I"]
