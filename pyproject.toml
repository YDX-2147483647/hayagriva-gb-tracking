[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[dependency-groups]
dev = [
    "click>=8.3.1",
    "httpx>=0.28.1",
    "regex>=2025.11.3",
    "tomlkit>=0.13.3",
]

[tool.ruff]
src = ["tracking"]

[tool.ruff.lint]
extend-select = ["U", "TYP", "F", "I"]
